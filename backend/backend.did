type Result = 
 variant {
   err: text;
   ok: Product;
 };
type Product = 
 record {
   brand: text;
   categories: text;
   image_url: text;
   name: text;
 };
service : {
  lookupProduct: (text, Product) -> (Result);
  scanBarcode: (text, Product) -> (Result);
}
